@model IEnumerable<BTL_Nhom13.Models.DanhMuc>
@{
    ViewBag.Title = "Category";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

@section Title{
    Danh mục
}
<div class="container-fluid">
    <a href="@Url.Action("Create", "AdminDanhMucs")" class="btn btn-outline-primary" role="button" aria-pressed="true">Thêm mới</a>
    <div class="row">
        <div class="col-md-12">
            <div class="card">
                <div class="card-header card-header-primary">
                    <h4 class="card-title">Quản lý danh mục</h4>
                    <p class="card-category">Danh sách các danh mục</p>
                </div>
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table table-hover">
                            <thead class="text-primary">
                            <th>Mã danh mục</th>
                            <th>Tên danh mục</th>
                            <th>Hành động</th>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>@Html.DisplayFor(modelItem => item.MaDM)</td>
                                        <td>@Html.DisplayFor(modelItem => item.TenDM)</td>
                                        <td>
                                            @Html.ActionLink("Sửa", "Edit", new { id = item.MaDM }) |
                                            @Html.ActionLink("Xóa", "Delete", new { id = item.MaDM })
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

